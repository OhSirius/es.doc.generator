<Window x:Class="BG.Application.DocumentsGenerator.LicenseKey"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BG.Application.DocumentsGenerator"
        mc:Ignorable="d"
        Title="Введите ключ из файла" Width="300" Height="59.54">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBox Grid.Row="0" Grid.Column="0"  Margin="3" IsReadOnly="True" Text="{Binding Key}" />        
        <Button Grid.Column="1" Grid.Row="0" HorizontalAlignment="Right" 
            MinWidth="80" Margin="3" Content="Установить!" Click="SetKey_Click" />
    </Grid>
</Window>
